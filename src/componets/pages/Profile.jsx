import React from "react";
import useTitle from "../../hooks/useTitle";

const Profile = () => {
  useTitle("Profile");
  return <div className="container mx-auto  h-screen">Profile</div>;
};

export default Profile;
